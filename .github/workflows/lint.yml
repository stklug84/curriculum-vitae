---
# title: Lint workflow
# authors:
#   - Steffen Klug <45033201+stklug84@users.noreply.github.com>
# date: 2026-06-11
# keywords: [infrastructure, linting]
# comment: |
#   Lints the repository on pull requests against main:
#     - actionlint   : .github/workflows/* (incl. shellcheck on run: steps)
#     - yamllint     : all YAML files (.yamllint.yml)
#     - markdownlint : all Markdown files (.markdownlint.yml)
#     - hadolint     : .github/docker/texlive/Dockerfile
#     - cv-schema    : validate each data/cv-*.yml against cv/parse schema
# ---
name: Lint

on:
  pull_request:
    branches:
      - main
  workflow_dispatch:

permissions:
  contents: read

jobs:
  actionlint:
    name: actionlint (workflows)
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v7

      - name: Download actionlint
        id: get-actionlint
        env:
          ACTIONLINT_VERSION: 1.7.10
        run: |
          set -euo pipefail
          BASE="https://raw.githubusercontent.com/rhysd/actionlint"
          bash <(curl -fsSL "$BASE/v${ACTIONLINT_VERSION}/scripts/download-actionlint.bash") \
            "$ACTIONLINT_VERSION"

      - name: Run actionlint
        run: |
          set -euo pipefail
          ./actionlint -color

  yamllint:
    name: yamllint
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v7

      - name: Install yamllint
        run: |
          set -euo pipefail
          pip install --user yamllint

      - name: Run yamllint
        run: |
          set -euo pipefail
          yamllint --strict .

  markdownlint:
    name: markdownlint
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v7

      - name: Run markdownlint
        uses: DavidAnson/markdownlint-cli2-action@ded1f9488f68a970bc66ea5619e13e9b52e601cd  # v23.2.0
        with:
          config: .markdownlint.yml
          globs: '**/*.md'

  hadolint:
    name: hadolint (Dockerfile)
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v7

      - name: Run hadolint
        uses: hadolint/hadolint-action@2332a7b74a6de0dda2e2221d575162eba76ba5e5  # v3.3.0
        with:
          dockerfile: .github/docker/texlive/Dockerfile

  cv-schema:
    name: cv-schema (${{ matrix.source }})
    runs-on: ubuntu-latest
    strategy:
      fail-fast: false
      matrix:
        # Every CV data source feeding the variant matrix
        # (data/variants.yml) must satisfy the cv/parse schema contract.
        source:
          - data/cv-academics.yml
          - data/cv-databricks.yml
    steps:
      - name: Checkout
        uses: actions/checkout@v7

      # Validate the source of truth against the cv/parse schema
      # contract; --check writes nothing and fails the PR on the first
      # violation with a clear message.
      - name: Validate ${{ matrix.source }}
        uses: stklug84/actions/cv/parse@cd9b63fdb53b6530c4cecdace6bd0fbc07964853  # v2.4.0
        with:
          source: ${{ matrix.source }}
          check: "true"
          # out-dir is required by the action but unused in --check mode.
          out-dir: .
